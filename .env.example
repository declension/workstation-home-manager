# Copy to `.env` (gitignored) and fill in — never commit `.env` itself.
#
# Comma-separated list of unix usernames that identify a work machine.
# git.nix uses this to pick the work vs personal git identity for $USER.
HM_WORK_USERNAMES=your-work-username
